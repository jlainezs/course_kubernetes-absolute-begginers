# --------------------------------------
# Sample pod
#   Prepares a pod with an nginx container
# --------------------------------------
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
    tier: frontend
spec:
  containers:
    - name: nginx
      image: nginx
